<head>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.20/dist/sweetalert2.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.20/dist/sweetalert2.min.css">
</head>
<style>
    /* Style the form container */
   .form-container {
        width: 80%;
        margin: 0 auto;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-shadow: 0 0 10px #ccc;
        background-color: #f9f9f9;
    }

    /* Style the form fields */
    .form-container input[type="text"],
    .form-container input[type="number"], 
    .form-container input[type="email"] ,
    .form-container input[type="file"],
   .form-container input[type="textarea"]
{
        width: 100%;
        padding: 12px 20px;
        margin: 8px 0;
        display: inline-block;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-sizing: border-box;
    }

    /* Style the submit button */
    .form-container input[type="submit"] {
        background-color: #4CAF50;
        color: white;
        padding: 14px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        float: right;
    }

    /* Clear floats after the form */
    .form-container::after {
        content: "";
        clear: both;
        display: table;
    }

    h1 {
        font-size: 36px;
        font-family: Arial, sans-serif;
        text-align: center;
    }

    textarea {
        min-height: 100px; /* hauteur minimale */
        height: auto; /* hauteur automatique */
        max-height: 300px; /* hauteur maximale */
        overflow-y: auto; /* barre de défilement automatique en cas de dépassement */
        resize: vertical;
    }
    .error-message {
        color: red;
    }


</style>

<div class="form-container">
    {{ form_start(formEquipe, {'method': 'PUT'}) }}
  
    Nb:L'ajout s'éffectue une seule fois puis vous pouvez modifier seulement la page .<br>
    
    <div class="form-group">
        {{ form_label(formEquipe.historique, 'Historique de l\'équipe') }}
        {{ form_widget(formEquipe.historique, {'attr': {'class': 'form-control form-description', 'placeholder': "Historique de l'equipe", 'style': 'width: 100%; margin: 0 auto;', 'rows': '5' }}) }}
            <span style="color:red;">{{ form_errors(formEquipe.historique) }}</span>

    </div>
    
    <div class="form-group">
        {{ form_label(formEquipe.objectifs, 'Objectifs de l\'équipe') }}
        {{ form_widget(formEquipe.objectifs, {'attr': {'class': 'form-control form-description', 'placeholder': "Objectifs de l'equipe", 'style': 'width: 100%; margin: 0 auto;', 'rows': '5' }}) }}
        <span style="color:red;">{{ form_errors(formEquipe.objectifs) }}</span>

    </div>

   <div class="form-group">
    {{ form_label(formEquipe.image, 'Image de notre équipe') }}
    {{ form_widget(formEquipe.image, {'attr': {'class': 'form-control'}}) }}
    {% if form_errors(formEquipe.image) %}
        <span style="color:red;">{{ form_errors(formEquipe.image) }}</span>
    {% endif %}
</div>
  
    <input type="submit" value="{{valeurBtnSubmit|default("Ajouter infos") }}" class="btn btn-primary" formnovalidate>
  
    {{ form_end(formEquipe) }}
</div>
{# // <script>
//   const form = document.querySelector('.form-container form');

//   form.addEventListener('submit', (event) => {
//     const historique = form.querySelector('#{{ formEquipe.historique.vars.id }}');
//     const objectifs = form.querySelector('#{{ formEquipe.objectifs.vars.id }}');
//     const image = form.querySelector('#{{ formEquipe.image.vars.id }}');

//     if (!historique.value || !objectifs.value || !image.value) {
//       event.preventDefault();

//       Swal.fire({
//         icon: 'error',
//         title: 'Remplir tous les champs, s\'il vous plaît!',
//       });
//     }
//   });
// </script> #}